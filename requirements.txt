pandas==2.1.1  # Pandas for handling DataFrames
openpyxl==3.1.2  # Openpyxl for reading Excel files (required by pandas for xlsx files)
# sqlite3 is included with Python, no need to install it separately
streamlit==1.19.0  # Streamlit for building the app

