pandas      # Pandas for handling DataFrames
openpyxl==3.1.2    # Openpyxl for reading Excel files (required by pandas for xlsx files)
#sqlite3==3.43.2    # SQLite for interacting with SQLite databases (comes with Python)
streamlit==1.25.0  # Streamlit for building the app

